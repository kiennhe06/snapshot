// Copy this file to `spotify_config.dart` (gitignored) and fill in your own
// Spotify app credentials from https://developer.spotify.com/dashboard.
// The real file is never committed because this repo is public.
const String spotifyClientId = 'YOUR_SPOTIFY_CLIENT_ID';
const String spotifyClientSecret = 'YOUR_SPOTIFY_CLIENT_SECRET';
